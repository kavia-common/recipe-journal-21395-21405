# React frontend environment variables for Recipe Journal
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
REACT_APP_SITE_URL=http://localhost:3000
# Backend base URL for proxy endpoints (FastAPI). If deployed at different origin, set full URL.
REACT_APP_BACKEND_BASE_URL=http://localhost:8000
